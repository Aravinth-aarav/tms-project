version: "3"

services:
  mongodb:
    image: mongo
    ports:
      - "27017:27017"

  server:
    build: ./server
    ports:
      - "5000:5000"
    env_file:
      - ./server/.env

  client:
    build: ./client
    ports:
      - "3000:80"
    depends_on:
      - server
